package test_classes;

public class Locators {
    public static void main(String[] args) {

        /*
         What is locator(s) ?
         A locator is used to identify and locate specific web elements within a web page
         Locators are used to find and select web elements so that you can perform actions on them, such as clicking,
         typing text.

                              There are 8 type of Locators:

         1. ID: To locate element by  "id" attribute, which is typically unique on a web page.

         2. Name: To locate element by "name" attribute, which may or may not be unique.

         3. Class Name: To locate element by "class" attribute, which can be shared by multiple elements.

         4. Tag Name:To locate element by the HTML tag name, such as "div," "a," "input," etc.

         5. Link Text: To locate anchor(link) element by Link text  (visible text between tags).

         6. Partial Link Text: To locate anchor(link) element by a portion of Link text (visible text between tags).

         7. XPath: Using XPath expressions to navigate the XML structure of the web page and select elements.

         8. CSS Selector: To locate elements based on their styles and attributes.
         */
    }
}
